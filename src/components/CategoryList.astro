---
import { getAllCategories } from '@/utils'

const categories = await getAllCategories();
---

<div class="wrapper">
    <h1>Kategorien</h1>
    <p>Klicke auf eine Kategorie um alle Rezepte in dieser Kategorie zu sehen</p>

    <ul>
        {
            categories.map((category) => (
            <li>
                <a href={`/categories/${category}`}><span class="tag">{ category }</span></a>
            </li>))
        }
    </ul>
</div>

<style>
    .wrapper {
        grid-area: recipeHeader;
        text-align: center;
        height: 30vh;
        width: 50%;
        margin: auto;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }
    
    h1 {
        margin-top: 2em;
    }

    p {
        margin-bottom: 2em;
    }
</style>